# Vibe / Mistral adapter

Read and follow the *entire* `AGENTS.md` in the folder above this one (the workspace root), starting with its "First-run detection" and "Start here — before any task" sections.

The root `AGENTS.md` is the source of truth for this workspace. If this file conflicts with it, the root `AGENTS.md` wins.

Canonical command and skill content lives in `../.agents/`. Don't duplicate shared instructions here.
