# Vibe / Mistral adapter

Read and follow the entire `AGENTS.md` in the folder above this one, starting with "First-run
detection" and "Start here - before any task."

The root `AGENTS.md` is the source of truth for this workspace. If this file conflicts with it,
the root `AGENTS.md` wins.

Canonical command and skill content lives in `../.agents/`. Do not duplicate shared instructions
here.
