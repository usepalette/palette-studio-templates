# Gemini adapter

Read and follow the *entire* `AGENTS.md` in this folder, starting with its "First-run detection" and "Start here — before any task" sections.

`AGENTS.md` is the source of truth for this workspace. If this file conflicts with `AGENTS.md`, `AGENTS.md` wins.

Canonical command and skill content lives in `.agents/`. Don't duplicate shared instructions here.
